# Youmi — المرحلة التالية: ربط Admin وMerchant بالـ API

تم ربط الواجهات الحالية بطبقة PHP/MySQL الجديدة مع الحفاظ على تصميم React الحالي.

## ما تم ربطه
- جلسة المستخدم: `me`, `login`, `register`, `logout`.
- المتاجر العامة من MySQL.
- متاجر البائع الخاصة به فقط.
- إنشاء متجر وربطه تلقائياً بحساب البائع.
- حفظ إعدادات المتجر من Merchant.
- CRUD المنتجات والكوبونات.
- تحديث حالات الطلبات من Merchant وAdmin.
- إنشاء الطلب مع التحقق من المخزون على الخادم.
- تتبع الطلب.
- اشتراك البائع وتجديده.
- لوحة Admin: المتاجر، المنتجات، الطلبات والإحصائيات من قاعدة البيانات.
- إدارة حالة البائع: active / suspended.
- منع البائع الموقوف من تنفيذ عمليات Merchant.
- إزالة الاعتماد على حفظ كل المتاجر دفعة واحدة من الواجهة؛ localStorage أصبح cache فقط.

## قاعدة البيانات
`public/api.php` ينشئ الجداول المطلوبة تلقائياً عند أول طلب API، ويحاول إضافة `users.status` إذا كانت القاعدة موجودة من إصدار سابق.

## InfinityFree
1. أنشئ `public/config.php` من `public/config.example.php`.
2. أدخل بيانات MySQL الصحيحة.
3. ارفع ملفات `dist` بعد تنفيذ `npm run build` إلى `htdocs`.
4. ارفع مجلد `public` بحيث يصبح `api.php` متاحاً في جذر الموقع.

> ملاحظة: لا تضع كلمة مرور المدير أو بيانات MySQL داخل React أو GitHub.
