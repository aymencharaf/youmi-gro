# Youmi — قاعدة بيانات مستقلة لكل بائع

تم تجهيز المشروع ليعمل بنمط Multi-Tenant مع **قاعدة بيانات مستقلة لكل متجر** عند تفعيل `tenant_databases_enabled`.

## ما الذي تغير؟

- قاعدة Youmi المركزية تحتفظ بالمستخدمين، المتاجر، الاشتراكات وإعدادات المنصة.
- عند وجود سجل في `tenant_databases`، تصبح منتجات/طلبات/كوبونات ذلك المتجر في قاعدة البائع.
- `syncStore()` لم يعد يحذف جميع المنتجات ثم يعيد إدخالها؛ يستخدم Upsert لتقليل عمليات MySQL.
- بيانات المتجر المركزية لا تحتفظ بنسخ ضخمة من المنتجات والطلبات والكوبونات بعد نقل المتجر.
- تمت إضافة `tenant_status` و `provision_tenant` للإدارة.

## مهم جداً مع InfinityFree

بعض الاستضافات المشتركة تمنع `CREATE DATABASE` من PHP. لذلك النسخة لا تفترض أن إنشاء قاعدة جديدة مسموح.

1. أنشئ قاعدة MySQL للبائع من لوحة الاستضافة.
2. أضف بياناتها إلى جدول `tenant_databases`.
3. ضع بيانات الدخول في `public/config.php` إذا كانت بيانات الدخول موحدة، أو عدّل السجل ببيانات قاعدة البائع.
4. فعّل `tenant_databases_enabled => true`.
5. نفّذ `provision_tenant` من حساب المدير أو استخدم SQL/أداة الاستضافة لإنشاء الجداول.

### إذا كان الخادم يسمح بإنشاء قواعد من PHP

يمكن لحساب المدير استدعاء: `public/api.php?action=provision_tenant` مع `storeId`.
سيتم إنشاء قاعدة باسم مثل `youmi_tenant_store_123` ثم ربطها بالمتجر.

## ترحيل متجر موجود

الطريقة الآمنة:

1. خذ نسخة احتياطية من قاعدة Youmi.
2. فعّل قاعدة البائع وسجلها في `tenant_databases`.
3. نفّذ `provision_tenant`.
4. `syncStore()` سينقل المنتجات والكوبونات والطلبات التي يراها في المتجر إلى قاعدة البائع.
5. بعد التأكد من نجاح المتجر، تصبح قراءة بياناته من قاعدة البائع.

## لماذا لم أجعل كل بائع يستخدم قاعدة جديدة تلقائياً على InfinityFree؟

لأن ذلك قد يفشل إذا كان حساب MySQL لا يملك صلاحية `CREATE DATABASE` أو لديه حد لعدد قواعد البيانات. النسخة الجديدة تدعم العزل الحقيقي دون جعل نشر Youmi ينكسر على الاستضافة المشتركة.

## النتيجة

البائع A → `youmi_tenant_store_A`

البائع B → `youmi_tenant_store_B`

البائع C → `youmi_tenant_store_C`

وقاعدة Youmi المركزية تبقى خفيفة مقارنة بالنسخة السابقة.


## التطبيق الفعلي في API
عند تفعيل `tenant_databases_enabled` وربط متجر بجدول `tenant_databases`، يستخدم `public/api.php` قاعدة المتجر للمنتجات والطلبات والكوبونات والاشتراكات، بينما تبقى بيانات تعريف المتجر والمستخدمين مركزية. إذا لم توجد قاعدة بائع مربوطة، يعمل المتجر تلقائياً على القاعدة المركزية كخطة توافق.

### ربط قاعدة بائع
من حساب المدير يمكن استدعاء `public/api.php?action=admin_configure_tenant` وإرسال JSON يحتوي: `storeId`, `dbHost`, `dbName`, `dbUser`, `dbPass`. يجب أن تكون قاعدة البيانات قد أُنشئت مسبقاً في الاستضافة، خصوصاً على الاستضافات المشتركة التي تمنع `CREATE DATABASE`.
